[phases.setup]
nixPkgs = ["python310", "nodejs_20", "curl"]

[phases.install]
pm = "npm"

[start]
command = "cd frontend && npm install && npm run build && cd ../backend && pip install -r requirements.txt && uvicorn app:app --host 0.0.0.0 --port $PORT"
